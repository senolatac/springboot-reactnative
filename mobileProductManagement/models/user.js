export default class User {
  constructor(id, name, username, password, token, role){
    this.id = id;
    this.name = name;
    this.username = username;
    this.password = password;
    this.token = token;
    this.role = role;
  }
}
